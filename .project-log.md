# Project Log

Internal project history and task tracker for Claude Code sessions.

> **Internal only.** This file is committed to GitHub but is **not deployed**.
> It lives at the project root, outside `src/` and `public/`, so neither
> Astro nor Cloudflare Pages can serve it. Keep it that way.

> **Before starting any work**, read this file first.
> **Before ending any work**, update this file with completed work,
> unfinished work, and next actions.
> **Keep entries short, useful, and factual.**

---

## Current Priorities

- Ship the combined performance + docs + project-log + video-mandate backport from this session (currently uncommitted, build verified)
- Per-client launch checklist (see `.claude/skills/performance.md` Section 12)

---

## Unfinished / To Do

- [ ] **Commit + push the combined backport.** ~20+ files changed locally; build is clean and verified. Awaiting explicit "push" command per memory rule (Git workflow — wait for command). Includes performance backport, docs page rewrite, project-log protocol, and the mandatory video-format rule.
- [ ] **Per-client deploy assets** (only relevant when actually deploying a client clone):
  - [ ] Drop `public/images/contact-hero.webp` (1920×800, ~80 KB)
  - [ ] Drop `public/og-image.webp` (1200×630)
  - [ ] Set `site:` in `astro.config.mjs` to the production domain
  - [ ] Set the four `PUBLIC_*` env vars on Cloudflare Pages → Build scope
  - [ ] Click "Retry deployment" after env vars are saved
- [ ] **At launch** — flip `noindex` default in `BaseLayout.astro` from `true` → `false` so non-thank-you pages become indexable.
- [ ] **Post-domain** — migrate from deferred GTM to Cloudflare Zaraz (see `.claude/skills/performance.md` Section 10). Eliminates 150–220 KB of browser-side JS.
- [ ] **Optional** — assess `npm audit` results from the Astro 6.1.9 bump (6 moderate transitive vulnerabilities, likely dev-only via fluent-ffmpeg).

---

## Blocked

- _(nothing currently)_

---

## Change Log

### 2026-04-30 (afternoon — docs page rewrite + mandatory video rule)

**Status:** in progress (changes locally; awaiting push)
**Task summary:** Rewrote the internal docs page (`src/pages/index.astro`) to match everything currently in `CLAUDE.md`, the agents, the skills, and the recent performance backport. Added 7 new article sections (Modes & Tiers, Skills Library, Performance & Build Tooling, Hero Video, Project Log Protocol, Env Variables, Cloudflare Deploy step-by-step). Added a mandatory video-format rule (self-hosted MP4 H.264 only) to CLAUDE.md and the hero-video skill. Restructured the Client Setup Checklist into Phase 1 (Marketing Inputs Required) and Phase 2 (Dev Build Steps). Renumbered existing sections 17–20 to 23–26. Removed legacy Netlify Forms script comment. Refreshed Section 16 Agents (now 12 with `performance-optimisation`, plus escalation rules and parallel/sequential waves).
**Files changed:**
- `src/pages/index.astro` — 1818 → 2894 lines (+1076 lines of docs)
- `CLAUDE.md` — new mandatory Video non-negotiable
- `.claude/skills/hero-video.md` — added MANDATORY callout at the top
- `.project-log.md` — this file
**Notes:** Wrote every new section for non-technical readability — Plain English callouts, step-by-step numbered lists, before/after comparison cards, glossary callouts. Verified all 27 section anchors render in `dist/client/index.html`. Stale TS diagnostics from the Netlify legacy script are gone (script removed). Build clean on Astro 6.1.9.
**Next action:** Commit + push when user gives the command.

---

### 2026-04-30 (morning — performance backport)

**Status:** in progress (changes locally; awaiting push)
**Task summary:** Site-wide performance backport from Northern Medical → base template. Adds Cloudflare adapter ASSETS-binding patch, dual-preload hero images, deferred GTM loader, JSON-LD `schema` prop, optional self-hosted MP4 hero video, `lang="en-GB"`, `noindex={true}` default, `media-src 'self'` CSP, 1-year cache for `/images`/`/videos`/`/icons`, ASCII-only robots.txt, `_redirects` placeholder, three resize/optimise scripts. Bumped Astro 6.1.3 → 6.1.9 with `vite ^7` override.
**Files changed:**
- `scripts/patch-cloudflare-adapter.mjs` (new)
- `scripts/resize-oversized-images.mjs` (new)
- `scripts/optimise-hero-video.mjs` (new)
- `package.json` — postinstall hook, resize/optimise scripts, sharp + ffmpeg dev deps, vite override
- `public/_headers` — `media-src 'self'`, `/icons`, `/videos`, 1-yr `/images`
- `public/_redirects` (new) — valid-status-code documentation
- `public/robots.txt` — ASCII-only
- `src/layouts/BaseLayout.astro` — lang=en-GB, noindex default true, heroImageMobile, schema prop, deferred GTM, media-src
- `src/pages/index.astro` — lang=en-GB
- `src/components/Hero.astro` — optional `video` prop, JS-injected source pattern
- `.claude/skills/hero-video.md` (new)
- `.claude/skills/performance.md` (new)
- `.project-log.md` (new — this file)
**Notes:** Build verified clean on Astro 6.1.9 + Vite 7.3.2. Smoke-test confirmed lang=en-GB, noindex, media-src, ccemail, deferred GTM all rendering correctly.
**Next action:** When user gives the push command, commit as a single feat() commit and push to origin/master.

### 2026-04-30 (earlier)

**Status:** completed (commit 9fb6a9a, pushed)
**Task summary:** Production-grade contact page using the canonical Web3Forms pattern. Inline form with the production-tested field contract (ccemail/semicolon/no `to`/`recaptcha_response`/replyto-CRLF-strip/isSubmitting guard). MedicalClinic JSON-LD. PageHero component. ContactForm.astro fixed to canonical pattern. Reviewer fixes for a11y/security/seo/conversion applied.
**Files changed:** `src/pages/contact.astro`, `src/components/PageHero.astro`, `src/components/ContactForm.astro`, `src/layouts/BaseLayout.astro`, `src/pages/thank-you.astro`, `.env.example`, `.claude/skills/contactform.md`
**Notes:** 7 files / 1,628 insertions. Skill doc captures the Web3Forms field contract and full per-client setup. See commit message for the full breakdown.
**Next action:** None for this work; superseded by the in-progress performance backport above.

---

## Session Notes

### 2026-04-30

- The Vite override (`"overrides": { "vite": "^7" }`) was added defensively — current resolution is 7.3.2, no Vite 8 in the tree, but the override prevents a future `npm install` from upgrading.
- Confirmed `BaseLayout.astro` cannot reference `import.meta.env.PUBLIC_SITE_URL` directly — Vite's static env replacement combined with const declarations there triggers misleading "Unterminated string literal" errors. Use `Astro.site` with `Astro.url.origin` fallback instead. (Documented in `.claude/skills/contactform.md`.)
- The Cloudflare adapter ASSETS-binding patch is wired via `npm postinstall` and is idempotent. Re-running reports `0 replacements, 5 already-done`.
- Astro 6.1 release brought no breaking changes. Sharp codec defaults, advanced SmartyPants, i18n fallback routes — none warranted code changes for clinic sites.
